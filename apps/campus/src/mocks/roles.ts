/** @deprecated Import from `lib/roles` or `lib/auth/role-matrix`. */
export {
  canEdit,
  canManage,
  canReviewHomework,
  canSchedule,
  canView,
  isAdminOrSuper,
  isTeacherAdminOrSuper,
  roleMatrix,
} from '../lib/auth/role-matrix';
