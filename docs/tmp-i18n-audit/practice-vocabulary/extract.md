# /practice/vocabulary extract → campus-strings

## Keys

`vocabulary.subtitle.*`, `mode.*`, `stat.*`, `status.*`, `filter.*`, `add.*`, `empty.*`, `remove.*`, `fc.*`, `play.*`, `detailsAria`, `removeAria`, `category.general` (reserved), `backAria`

## Surfaces

- `apps/campus/src/app/vocabulary/page.tsx` (+ re-export `practice/vocabulary`)
- Mode/Stats/Add/Filters/List/WordCards/Flashcard/Play sections
