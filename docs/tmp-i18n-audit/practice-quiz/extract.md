# /practice/quiz extract → campus-strings

Keys under `quiz.*` (hero, stats, cards, source, difficulty, play, result, delete/errors).

Surfaces: `app/quiz/page.tsx`, `QuizAssignmentCards`, `QuizPlaySession`, `QuizResultScreen`, `app/quiz/sections.tsx`.
