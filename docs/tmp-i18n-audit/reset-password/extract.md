# /reset-password extract

## campus-strings

`reset.updating`, `reset.back`, `reset.missingTokenSubtitle`, `reset.missingTokenBody`, `reset.requestNewLink`, `reset.error.missingToken|minLength|mismatch|failed`

## Code

- All chrome via `t()`
- Locale-aware links + post-success redirect to `/login?reset=success`
- Password field toggle labels from `login.show/hidePassword`
