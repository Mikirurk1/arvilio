P2.1 `/admin` (2026-07-16): wired admin chrome to CMS keys: `admin.*`, `admin.table.*`, `studentImport.*`.

- Done: page header/subtitle, metrics, create-account form, users table, student import panel.
- Kept as content/data: user names/emails, dates, timezone display strings, API error payload text.
- Verified at `/uk/admin` after seed (`npm run seed:campus-ui -w @app/cms`).
